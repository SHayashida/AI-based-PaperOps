name: ci

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up uv
        uses: astral-sh/setup-uv@v3
        with:
          version: 'latest'
      - name: Install LaTeX tools
        run: |
          sudo apt-get update
          sudo apt-get install -y latexmk texlive-latex-extra texlive-fonts-recommended texlive-bibtex-extra biber
      - name: Install
        run: uv sync
      - name: Run example
        run: make run
      - name: Build assets
        run: make assets-all
      - name: Sync references
        run: make refs-all
      - name: Build provenance ledger
        run: make provenance-all
      - name: Build claim ledger
        run: make claims-all
      - name: Review thread
        run: make review-all
      - name: Build reviewer packet
        run: make packet-all
      - name: Verify claims
        run: make verify-all
      - name: Build profile report
        run: make profile-all
      - name: Check
        run: uv run truthweave check --mode ci
      - name: Benchmark contracts
        run: make benchmarks
      - name: Build exemplar demos
        run: make exemplars
